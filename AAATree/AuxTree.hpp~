#ifndef SJTU_AUXTREE_HPP
#define SJTU_AUXTREE_HPP

#include "AuxNode.hpp"

template<class D, class T, class A, class M>

template<class T, class A, class M>
class AuxTree{
private:
	AuxNode *root;
};

#endif
